# Not required yet — the app runs fully on localStorage (src/lib/store.ts)
# until these are set. Fill in once a Supabase project exists.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional shared team PIN shown at login. Leave blank to skip the PIN step.
VITE_TEAM_PIN=
