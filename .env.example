# Not required yet — the app runs fully on localStorage (src/lib/store.ts)
# until these are set. Fill in once a Supabase project exists.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# The team PIN is no longer set here — it's stored hashed in the database
# (app_config table) and checked server-side. See
# supabase/migrations/0023_secure_registration.sql to view or change it.
